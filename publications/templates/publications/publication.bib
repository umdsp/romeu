@{{ publication.type.bibtex_type }}{% templatetag openbrace %}{% if publication.citekey %}{{ publication.citekey }}{% else %}{{ publication.key }}{% endif %},
  author = "{{ publication.authors }}",
  title = "{{ publication.title }}",
  year = {{ publication.year }}{% if publication.journal %},
  journal = "{{ publication.journal }}"{% endif %}{% if publication.book_title %},
  booktitle = "{{ publication.book_title }}"{% endif %}{% if publication.publisher %},
  publisher = "{{ publication.publisher }}"{% endif %}{% if publication.volume %},
  volume = {{ publication.volume }}{% endif %}{% if publication.institution %},
  institution = {{ publication.institution }}{% endif %}{% if publication.number %},
  number = "{{ publication.number }}"{% endif %}{% if publication.chapter %},
  chapter = "{{ publication.chapter }}"{% endif %}{% if publication.pages %},
  pages = "{{ publication.pages }}"{% endif %}{% if publication.month_bibtex %},
  month = "{{ publication.month_bibtex }}"{% endif %}{% if publication.keywords %},
  keywords = "{{ publication.keywords }}"{% endif %}{% if publication.doi %},
  doi = "{{ publication.doi }}"{% endif %}{% if publication.url %},
  url = "{{ publication.url }}"{% endif %}{% if publication.note %},
  note = "{{ publication.note }}"{% endif %}{% if publication.isbn %},
  isbn = "{{ publication.isbn }}"{% endif %}{% if publication.issn %},
  issn = "{{ publication.issn}}"{% endif %}{% if publication.annote %},
  annote = "{{ publication.annote }}"{% endif %}{% if publication.edition%},
  edition = "{{ publication.edition}}"{% endif %}{% if publication.editor %},
  editor = "{{ publication.editor }}"{% endif %}{% if publication.how_published %},
  howpublished = "{{ publication.how_published }}"{% endif %}{% if publication.organization %},
  organization = "{{ publication.organization }}"{% endif %}{% if publication.address %},
  address = "{{ publication.address }}"{% endif %}{% if publication.university %},
  school = "{{ publication.university }}"{% endif %}{% if publication.series %},
  series = "{{ publication.series }}"{% endif %}{% if publication.archice_location %},
  lccn = "{{ publication.archive_location}}"{% endif %}{% if publication.abstract %},
  abstract = "{{ publication.abstract }}"{% endif %}{% if publication.price %},
  price = {{ publication.price }}{% endif %}{% if publication.rights %},
  copyright = "{{ publication.rights }}"{% endif %}{% if publication.language %},
  language = "{{ publication.language }}"{% endif %}{% if publication.table_of_content %},
  contents = "{{ publication.table_of_content }}{% endif %}
}
